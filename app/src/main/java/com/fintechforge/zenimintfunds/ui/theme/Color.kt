package com.fintechforge.zenimintfunds.ui.theme

import androidx.compose.ui.graphics.Color

// Colores Principales (Menta y Verde)
val MintPrimary = Color(0xFF00BFA5)   // Menta vibrante (Botones, acentos)
val MintDark = Color(0xFF008E76)      // Menta oscuro (Barras, fondos fuertes)
val MintLight = Color(0xFF64FFDA)     // Menta luz (Textos brillantes en modo oscuro)

// Colores Funcionales (Semáforo financiero)
val GreenSuccess = Color(0xFF66BB6A)  // Ingresos / Pagado
val RedError = Color(0xFFEF5350)      // Gastos / Deudas
val GoldWarning = Color(0xFFFFCA28)   // Alertas / Próximo a vencer

// Modo Oscuro (Elegante y profundo, no negro total)
val DarkBackground = Color(0xFF121212)      // Fondo general
val DarkSurface = Color(0xFF1E1E1E)         // Tarjetas
val DarkSurfaceVariant = Color(0xFF2D2D2D)  // Listas secundarias
val DarkTextPrimary = Color(0xFFEEEEEE)     // Texto principal
val DarkTextSecondary = Color(0xFFB0B0B0)   // Texto secundario

// Modo Claro (Limpio y minimalista)
val LightBackground = Color(0xFFF5F5F5)
val LightSurface = Color(0xFFFFFFFF)
val LightSurfaceVariant = Color(0xFFE0E0E0)
val LightTextPrimary = Color(0xFF212121)
val LightTextSecondary = Color(0xFF757575)